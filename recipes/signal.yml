app: signal-desktop
module: debian

mirror: https://updates.signal.org/desktop/apt/
version: xenial
repositories:
  - main
  
script:
  - ln -sfv ../lib/signal-desktop/signal-desktop usr/bin/signal-desktop